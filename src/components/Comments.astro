---
/**
 * Comments via Giscus, backed by GitHub Discussions.
 * No database, no spam handling, and no XSS surface - GitHub renders
 * the content inside its own iframe.
 *
 * Setup before this works:
 *   1. Enable Discussions in the repository settings on GitHub
 *   2. Install the Giscus app: https://github.com/apps/giscus
 *   3. Get repoId and categoryId from https://giscus.app and fill them in below
 */
const GISCUS = {
  repo: "koalalikecode/koalalikecode-blog",
  repoId: "", // from giscus.app
  category: "Announcements",
  categoryId: "", // from giscus.app
};

const configured = GISCUS.repoId !== "" && GISCUS.categoryId !== "";
---

{
  configured ? (
    <div class="giscus" />
  ) : (
    <p class="text-sm" style="color: var(--text-muted);">
      Comments are not configured yet. See the setup notes in
      <code
        class="rounded px-1 py-0.5 text-xs"
        style="background: var(--bg-subtle); border: 1px solid var(--border);"
      >
        src/components/Comments.astro
      </code>
      .
    </p>
  )
}

{
  configured && (
    <script
      is:inline
      src="https://giscus.app/client.js"
      data-repo={GISCUS.repo}
      data-repo-id={GISCUS.repoId}
      data-category={GISCUS.category}
      data-category-id={GISCUS.categoryId}
      data-mapping="pathname"
      data-strict="1"
      data-reactions-enabled="1"
      data-emit-metadata="0"
      data-input-position="top"
      data-theme="preferred_color_scheme"
      data-lang="en"
      data-loading="lazy"
      crossorigin="anonymous"
      async
    />
  )
}
